# Create the dictionary
my_dict = {
    "brand": "Ford",
    "model": "Mustang"
}

# Print the model
print(my_dict["model"])

# Add a color key
my_dict["color"] = "red"

# Remove the brand key
del my_dict["brand"]

# Print the dictionary
print(my_dict)